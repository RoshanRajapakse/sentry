# getsentry requirements
#
# these dependencies are unused inside `sentry`
# (if they become used in `sentry` please move them to `requirements-base.txt`)
#
# they are included in the open-source sentry repository to speed up
# requirements synchronization across the two repositories

Avalara==20.9.0
PlanOut==0.6.0
pycountry==17.5.14
pyvat==1.3.15
reportlab==3.6.1
stripe==2.61.0
certifi>=2023.7.22 # not directly required, pinned by Snyk to avoid a vulnerability
idna>=3.7 # not directly required, pinned by Snyk to avoid a vulnerability
requests>=2.31.0 # not directly required, pinned by Snyk to avoid a vulnerability
